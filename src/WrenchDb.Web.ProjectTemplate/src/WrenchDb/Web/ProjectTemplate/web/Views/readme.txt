Put here vm files that implements application views.
Each subfolder must have same name of the view . 
Each file name into that folder must respect convention:
    /viewname/region-viewname.vm
So you can replace one or more region of layout by adding file into that folder.

See on http://wrenchdb.codeplex.com/documentation for more info.