
       

          <h1>OOOK</h1> 

            $Model.getModelName() is great!
            

<table>
           #set ($map = $Model.getProperties() )
        #foreach ($mapEntry in $map.entrySet())
           <tr><td>$mapEntry.key</td>
            <td><value>$mapEntry.value</td></tr>
        #end

   </table>

